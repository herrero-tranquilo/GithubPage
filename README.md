# GithubPage — 게임 프로젝트

GitHub Pages로 배포되는 게임 프로젝트.

## 구조

```
02_Game_GithubPage/
├── project/    ← 소스코드
└── README.md
```

## 배포

`project/` 기준으로 GitHub Actions가 자동 배포.

- 브랜치: `master` push 시 자동 빌드 및 `gh-pages` 배포
